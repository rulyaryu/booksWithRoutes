<?php   

// $p = new Pagination();

// $startIdx = ($pageObj->getPage() - 1) * $pageObj->getPerpage();

// $booksOnPage = array_slice($booksArr, $startIdx, $pageObj->getPerpage());

// $chunkedBooks = array_chunk($booksOnPage, 3);
$chunkedBooks = array_chunk($booksArr, 3);